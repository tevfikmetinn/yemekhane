# Oturum Notu — 2026-05-16 (Cumartesi)

Yemekhane projesinin ilk kurulum oturumu. Bu dosya, kararları ve **pazartesi 18 Mayıs için kalan işleri** kaydetmek için.

## Bu oturumda tamamlananlar

- Git repo init, klasör iskeleti (`scraper/`, `data/foods/`, `data/menus/`, `images/foods/`, `site/`, `.github/workflows/`)
- Veri kaynağının yapısı analiz edildi: `https://unievi.firat.edu.tr/` ana sayfasında `<!--İçerik Başlangıç-->` ... `<!--İçerik Son-->` yorum etiketleri arası, **hafta içi**, dinamik menü slot'u
- Aylık PDF (`firat.edu.tr/documents/*.pdf`) kullanılmıyor (kullanıcı kararı)
- Python scraper (`scraper/`):
  - `normalize.py` — Türkçe metin standartlaştırma (case, aksan, noktalama)
  - `matcher.py` — `difflib`-tabanlı fuzzy alias match; eşikler: 0.88 auto, 0.70 suggest
  - `scrape.py` — HTML çek + slot regex + bileşen parse + match + JSON yaz
- Mock HTML ile test: typo'lara dayanıklı, bilinmeyenler graceful (çökme yok)
- Bileşen kütüphanesi (23 bileşen):
  - Çorba (7): yayla, mercimek, ezogelin, harput, dövme, domates, tavuk suyu, şehriye
  - Pilav/yan (3): pirinç pilavı, bulgur pilavı, peynirli makarna
  - Ana (4): tavuk kızartma, fırın tavuk, fırın köfte, nohut yemeği
  - Garnitür (1): patates kızartması
  - İçecek/kase (2): ayran, yoğurt
  - Salata (1): mevsim salatası
  - Tatlı/atıştırmalık (3): tahin helvası, generic tatlı, generic meyve
  - Ekmek (1)
- Statik frontend (`site/index.html`, `app.js`, `style.css`) — bordo tema, mobil uyumlu, toplam kalori/makro hesabı, disclaimer
- GitHub Actions:
  - `scrape.yml` — hafta içi her 15 dk polling (TR 08:00-13:00 ve 14:00-17:00)
  - `deploy.yml` — push'ta Pages'e yayınlar (`site/` + `data/` + `images/` birleştirip deploy eder)
- README + yasal feragatname
- Lokal HTTP testi yapıldı (`python -m http.server`)

## Pazartesi 18 Mayıs için yapılacaklar (öncelik sırasıyla)

### 1. Gerçek scrape testi (en kritik)
- **Saat:** TR 09:30 sonrası (menü siteye düşmüş olmalı, gecikmiş bile olsa).
- **Adım:** `python scraper/scrape.py` çalıştır, `data/current.json` üret.
- **Kontrol:**
  - HTML slot bulundu mu? (`status: ok` mu?)
  - Slot'taki gerçek HTML yapısı parser'ın beklediğiyle uyumlu mu? (Beklediğimiz `<li>`, `<td>`, `<p>`, `<div>` — birinde değilse selector eklemek lazım.)
  - Bileşenler doğru eşleşti mi? Eşleşmeyenler bilinmeyen mi?
- **Çıktı:** İlk gerçek menü JSON'u + (varsa) bilinmeyen log.

### 2. Parse ince ayarı
Gerçek HTML'in yapısı görüldüğünde scraper'ı spesifik selektörle hızlı/daha temiz hale getir. Şu an `find_all(["li", "td", "p", "div"])` geniş bir net atıyor; gerçek slot bilinince daraltabiliriz.

### 3. Eldeki fotoğrafları yerleştir
- `images/foods/` altına: `pirinc-pilavi.jpg`, `nohut-yemegi.jpg`, `firin-kofte.jpg`, `peynirli-makarna.jpg`, `ezogelin-corbasi.jpg`, `tahin-helvasi.jpg`
- 4-bölmeli tabak SS'inden bileşenleri kırp ve ayrı kaydet.

### 4. Admin paneli MVP
Hedef: kullanıcı bilinmeyen yemekleri panelden ekleyebilsin.
- Yaklaşım önerisi: `site/admin.html` — bilinmeyenler listesi (`data/unknown_items.json`'dan veya `current.json`'daki `needs_review: true`'lardan). "Ekle" → template form → submit → **iki seçenek**:
  - (a) JSON ve fotoğraf "indir", kullanıcı `data/foods/` ve `images/foods/`'a koyup commit'ler.
  - (b) GitHub PAT ile doğrudan REST API üzerinden commit (CSP/CORS izinleri OK).
- (a) hızlı, ücretsiz, manuel. (b) daha cilalı ama PAT yönetimi gerekir. (a) ile başla.

### 5. GitHub repo + Pages
- GitHub'da repo oluştur, `git push -u origin main`
- Settings → Pages → Source: GitHub Actions
- Actions sekmesinden `deploy` workflow'unun başarılı olduğunu doğrula
- Yayın URL'sini test et

### 6. İkinci öğün gözlemi
Kullanıcı kendisi ikinci öğüne genelde gitmediği için, pazartesi 15:00-16:00 arası bizzat sitede 2. öğün menüsünün ne zaman ve nasıl güncellendiğini gözlemleyecek. Davranışa göre `scrape.yml` cron'unu ince ayarlayabiliriz.

## Açık sorular / sonra düşünülecekler

- **2. öğün ayrımı:** Şu anki scraper ana sayfadaki tek slot'u okuyor, "öğle" vs "akşam" ayrımı yok. Site bunları ayrı mı gösteriyor? Gözleme bağlı. Olası şema değişikliği: `current.json` içine `meals: {lunch: [...], dinner: [...]}` yapısı.
- **Patates kızartması gibi garnitürler:** Menüde ayrı satır mı, ana yemekle aynı satırda mı geliyor? Görünce kararlaştırılacak (alias listesi vs ayrı bileşen).
- **Bağlamsal eşleştirme (`HELVA`, `TAHİN.H` gibi):** Alias listelerine eklendi ama daha sofistike sistem (kısaltma sözlüğü) gerekirse sonra.

## Mevcut bileşen envanteri (23 dosya)

```
ayran, bulgur-pilavi, domates-corbasi, dovme-corbasi, ekmek,
ezogelin-corbasi, firin-kofte, firin-tavuk, harput-corbasi,
mercimek-corbasi, mevsim-salatasi, meyve, nohut-yemegi,
patates-kizartmasi, peynirli-makarna, pirinc-pilavi,
sehriye-corbasi, tahin-helvasi, tatli, tavuk-kizartma,
tavuk-suyu-corbasi, yayla-corbasi, yogurt
```

## Eldeki fotoğraf envanteri

| Bileşen | Adet | Kalite | Not |
|---|---|---|---|
| Pirinç pilavı | 3-4 | Net | Doğrudan kullan |
| Nohut | 2 | Üstten, ışıklı | Adı kontrol: "etli nohut" mu "nohut" mu |
| Fırın köfte | 1 (SS) | Net (SS'in 1 bölmesi) | Kırpılacak |
| Peynirli makarna | 1 (SS) | Net | Kırpılacak |
| Ezogelin çorbası | 1 (SS) | Net | Kırpılacak |
| Tatlı (SS) | 1 | Net | Generic tatlı için kullan |
| Fırın tavuk + pilav | 1 | Karışık | Bileşen olarak değil banner için |
| Ayran | 1 | Marka belirgin | Generic kullan |
| Tahin helvası | 1 | Paket | Paket fotosu |
